let numbers = [5, 9, 3, 19, 70, 8, 100, 2, 35, 27];

let result = 0;

for (let index = 0; index < numbers.length; index += 1) {
  result += numbers[index]
}
console.log(result );

/* A mesma estrutura da atividade anterior, como pediu para printar na tela eu já fiz a soma pelo console.log mesmo, mas eu poderia criar uma variavel para guardar essa informação e printar a variavel em si assim deixando o codigo mais legivel */